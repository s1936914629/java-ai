spring:
  application:
    name: deepseek-util
  profiles:
    active: local  # 默认激活 local 环境（开发时）

# DeepSeek 公共配置
deepseek:
  base-url: https://api.deepseek.com/v1
  model: deepseek-chat
  timeout: 30
  # 注意：api-key 不在此文件配置，由具体环境的配置文件提供